#!/usr/bin/python3
# GENOCIDE - the king of the netherlands commits genocide - OTP-CR-117/19/001 - otp.informationdesk@icc-cpi.int - https://genocide.rtfd.io
#
#

from ol.spc import Console, cmd, execute, ol, scandir
from ol.spc import os, pwd, sys, time 

def daemon():
    pid = os.fork()
    if pid != 0:
        ol.trm.termreset()
        os._exit(0)
    os.setsid()
    os.umask(0)
    si = open("/dev/null", 'r')
    so = open("/dev/null", 'a+')
    se = open("/dev/null", 'a+')
    os.dup2(si.fileno(), sys.stdin.fileno())
    os.dup2(so.fileno(), sys.stdout.fileno())
    os.dup2(se.fileno(), sys.stderr.fileno())

def boot(name):
    ol.wd = os.path.expanduser("/var/lib/%s" % name)
    wait = False
    k = ol.krn.boot(name)
    k.cfg.wd = ol.wd
    k.cfg.md = os.path.join(ol.wd, "gmod")
    if "b" in k.cfg.opts:
        print("%s started at %s" % (name.upper(), time.ctime(time.time())))
        print(ol.format(k.cfg))
    return k

def main():
    k = boot("genocide")
    if k.cfg.txt:
        return cmd(k.cfg.otxt)
    if "d" in k.cfg.opts:
        daemon()
        privileges(name)
    scandir(k.cfg.md, "gmod")
    k.start()
    k.wait()
         
execute(main)
os._exit(0)
