#!/usr/bin/python3
# GENOCIDE - using the law to administer poison (bin/test)
#
# This file is placed in the Public Domain.

import os, sys ; sys.path.insert(0, os.getcwd())

import time
import unittest

from gcd.prs import parse_cli
from gcd.trm import execute

test_path = os.getcwd() + os.sep + "test"
wd = os.path.expanduser("~/.test")

def open(txt):
    try:
        for line in os.popen(txt).readlines():
            print(line.rstrip())
    except:
        pass

def main():
    open("rm -fR ~/.test")
    cfg.debug = True
    cfg.wd = wd
    parse_cli()
    if cfg.txt:
        pat = "test_%s*" % cfg.txt
    else:
        pat = "test*"
    suite = unittest.loader.TestLoader().discover(test_path, pattern=pat)
    unittest.TextTestRunner(verbosity=3).run(suite)

execute(main)
