# This file is placed in the Public Domain.

PYTHONPATH="lib:."
export PYTHONPATH
bin/clean
python3 setup.py sdist
python3 -mvirtualenv --system-site-packages env
env/bin/pip3 install genocide -f dist --upgrade --force-reinstall --no-cache-dir --no-deps
