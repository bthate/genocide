name: Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Prepare repo
        uses: actions/checkout@master
      - name: xmlrun
        uses: dev/xmlrun
      - name: Test
        uses: onichandame/python-test-action@master
