#!/usr/bin/env python3


import os
import sys


os.environ["PYTHONPATH"] = os.getcwd()


sys.path.insert(0, os.getcwd())


TODO = ["genocide", "test"]


import genocide


def popen(txt):
    for line in os.popen(txt).readlines():
        print(line.rstrip())


for i in TODO:
    os.chdir(i)
    popen("python3 -m doctest -o ELLIPSIS *.py")
